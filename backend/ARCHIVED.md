# Архив: FastAPI + PostgreSQL

Этот каталог **не используется** для продакшена Mini App на GitHub Pages.

Актуальный backend: `sheets-backend/` (Google Apps Script + Google Sheets + Drive).

Код оставлен как локальный архив / справочник контрактов. Не деплоить на Render/VPS для MVP.
